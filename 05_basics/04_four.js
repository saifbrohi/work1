const myobject = {
    js:"javascript",
    ruby:"ruby",
    jv:"java",
    php:"php laravel"
}

for (const key in myobject) {
    //console.log(`${key} Shortcut is For ${myobject[key]}`);
}

const programmming = ["java","php","node js","java"]

for (const key in programmming) {
    console.log(`${programmming[key]}`);
}